{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch main",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/main.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/build/bin",
            "environment": [],
            "preLaunchTask": "copy-assets"
        }
    ]
}
